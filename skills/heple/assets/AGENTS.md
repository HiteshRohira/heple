## Visual plans

Use the installed `$heple` skill when a plan benefits from hierarchy, steps, comparisons, risks, code hotspots, or other spatial presentation. Keep `plan.json` as the source of truth, require `heple validate plan.json` to pass, and use `--no-open` for non-interactive rendering.
